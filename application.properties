#logging.level.org.springframework.boot.autoconfigure=DEBUG

server.port: 9999
server.ssl.key-store: keystore.p12
server.ssl.key-store-password: @ptimal$y$
server.ssl.keyStoreType: PKCS12
server.ssl.keyAlias: server

security.basic.enable=false
server.tomcat.max-threads: 10
server.connection-timeout=10000


spring.datasource.url: jdbc:mysql://advintic-ops.ddns.net:3306/his_integ?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Optimal03ADV15SYS
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.dbHost=advintic-ops.ddns.net
spring.datasource.dbPort=3306

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project

spring.jpa.hibernate.ddl-auto = update

spring.jpa.properties.hibernate.type=trace 
# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect =org.hibernate.dialect.MySQL5Dialect

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

logging.file = server.log

spring.thymeleaf.cache=false
spring.thymeleaf.check-template=true
spring.thymeleaf.check-template-location=true
spring.thymeleaf.content-type=text/html
spring.thymeleaf.enabled=true
spring.thymeleaf.encoding=UTF-8
#spring.thymeleaf.mode=HTML
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.http.multipart.max-file-size=100MB
spring.http.multipart.max-request-size=100MB

hl7.host=advintic-ops.ddns.net
hl7.port=3332